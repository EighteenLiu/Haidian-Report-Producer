# 街道分报告程序逻辑

## 1. 程序目标

程序用于批量生成街道分报告成果，核心输出有三类：

1. 街道案件明细表
2. 街道环境建设管理工作运行情况分析报告
3. 专项表格汇总.xlsx

## 2. 输入来源

程序支持一次选择多个文件，输入类型包括：

- `.xlsx`
- `.docx`
- `.doc`
- `.wps`

输入文件会在左侧列表逐行显示。

程序会自动跳过：

- 以 `~$` 开头的临时文件
- 文件名包含“模板”的文件
- 路径中包含“提交格式”的文件

## 3. 主数据识别

生成主报告时，必须存在主数据 Excel。

主数据识别规则：

1. 扫描输入文件
2. 查找文件名包含 `现场检查用数据` 的 `.xlsx`
3. 找到后作为主数据源

如果找不到该文件，生成会直接报错。

## 4. 模板选择

模板分为两个独立输入项：

1. 案件明细表模板
2. 分析报告模板

两个模板互不干扰，程序按各自路径读取。
模板文件只读，不修改。

## 5. Word/WPS 材料识别

Word/WPS 文件名可以比较乱，程序不是靠文件名精确匹配，而是靠文件内表格和固定分类关键词识别内容。

当前主要分类关键词包括：

- 道路清扫保洁
- 垃圾管理
- 城市家具
- 背街小巷
- 网格化主动治理
- 综合执法

## 6. 报告内容构造

程序为每个街道构造一个 `StreetReportContext`，里面包含：

- `normal_management`
- `waste_management`
- `city_furniture`
- `backstreets`
- `grid_management`
- `law_enforcement`
- `suggestions`
- `report_sections`

### 6.1 环境常态化管理

由主数据和案件明细统计拼出：

- 问题发现情况
- 问题解决情况
- 道路清扫保洁作业

### 6.2 垃圾管理

由“垃圾管理”相关 Word/WPS 表格拼出：

- 问题发现和解决情况
- 小卫星案件情况
- 建筑垃圾综合治理

### 6.3 城市家具治理

如果相关表格没有数据，则该段直接为空。

### 6.4 背街小巷治理

如果相关表格没有数据，则该段直接为空。

### 6.5 网格治理

如果相关表格没有数据，则该段直接为空。

### 6.6 综合执法

由综合执法相关表格生成两段正文。

### 6.7 工作建议

建议段由前面各专题是否有内容决定。
当前为三条建议，且会按实际存在内容动态输出。

## 7. 报告模板渲染

分析报告模板已经改成动态结构，不再把所有专题标题写死。

新的渲染规则是：

1. 先按内容组装 `report_sections`
2. 只输出有内容的专题
3. 被省略的专题不占位
4. 后续专题编号自动重排

也就是说，城市家具、背街小巷、网格、综合执法、工作建议都会按实际内容顺延编号，不再依赖固定章节号。

## 8. 专项表格汇总

专项表格汇总.xlsx 的生成逻辑：

1. 从 Word/WPS 材料中提取表格
2. 每张表单以表格标题优先作为 sheet 名
3. 没有标题时回退为 `文件名-序号`
4. 每个 sheet 自动适应列宽

## 9. 输出目录

每个街道生成一个独立子目录，内含：

- `{月份}{街道}案件明细表.xlsx`
- `{月份}{街道}环境建设管理工作运行情况分析报告.docx`

根目录还会生成：

- `专项表格汇总.xlsx`

## 10. 前端逻辑

- 左侧使用较大的多行文本框展示已上传文件
- 模板文件分开选择，不再共用一个入口
- 生成过程中显示绿色进度条
- 生成结束后显示用时统计

## 11. 发布目录结构

打包后的发布目录按用途分类：

- 根目录：`街道分报告生成器.exe`
- `templates`：随程序发布的模板文件
- `docs`：程序使用指南、程序逻辑、README 等说明文件
- `_internal`：运行依赖文件

程序打包后会优先从 exe 同级的 `templates` 目录读取默认模板；源码运行时仍从项目根目录读取默认模板。

## 12. 维护约束

1. 改代码后要同步检查 README、程序使用指南、程序逻辑等说明文件。
2. 改模板或编号规则时，要检查旧逻辑是否仍在生效。
3. 临时文件不要写入 `C:`
4. 未明确要求时，不要额外打包压缩
