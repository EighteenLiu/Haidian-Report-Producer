# 街道分报告程序开发说明

## 1. 项目目标

本程序用于批量生成街道分报告相关成果，核心输出为：

1. 街道案件明细表
2. 街道环境建设管理工作运行情况分析报告
3. 专项表格汇总.xlsx

## 2. 前端要求

- 左侧按行显示两个模板文件栏、输出目录和已上传文件列表
- 已上传文件列表使用较大的多行文本框，约 6 行高
- “添加文件”支持一次多选
- 去掉“选择目录”式的主入口，改为“添加文件”
- 右侧按钮竖向排列
- 进度条使用绿色
- 生成结束后显示生成用时

## 3. 输入识别

支持的输入文件：

- `.xlsx`
- `.docx`
- `.doc`
- `.wps`

自动跳过：

- 临时文件 `~$*`
- 文件名包含“模板”的文件
- 路径中包含“提交格式”的文件

主数据文件要求：

- 文件名必须包含 `现场检查用数据`
- 找不到时直接报错

## 4. 模板文件

模板文件分为两个独立路径：

1. 案件明细表模板
2. 分析报告模板

模板只读，不修改。

## 5. 报告模板动态逻辑

分析报告模板已经从“写死章节”改为“按内容动态书写”。

新的规则是：

- 只生成有内容的专题
- 空专题直接省略
- 被省略的专题不占编号
- 后续专题自动重排

例如：

- 如果“城市家具治理”为空
- 后面的“背街小巷治理”会自动变成“三、背街小巷治理”

这意味着模板和代码两边都不能再依赖固定章节序号。

## 6. 专项表格汇总逻辑

- 每个 sheet 自动适应列宽
- sheet 名优先使用表格标题
- 没有标题时回退为 `文件名-序号`

## 7. 维护要求

- 修改代码后要同步更新 README、程序使用指南、程序逻辑等说明文件
- 修改逻辑时要检查旧逻辑是否仍在生效，防止冲突
- 不要把临时文件写进 `C:`
- 未明确要求时不要额外压缩打包
